interface Action {
  type: string;
  skill?: string;
  name: string;
  actionPointDuration: number;
  durabilityUsed: number;
}

const action: Action = {
  type: 'string',
  skill: 'string',
  name: 'string',
  actionPointDuration: 0,
  durabilityUsed: 0,
};
action;
